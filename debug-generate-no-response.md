# [OPEN] debug-generate-no-response

## 症状
- 点击“立即生成”后无响应。

## 假设
1. 前端点击事件没有真正发起请求。
2. 前端发起了请求，但请求参数校验失败并被静默吞掉。
3. 后端生成接口路由或依赖注入异常，导致请求未进入核心逻辑。
4. 前端请求已发送，但因 CORS / 网络错误 / 超时没有反馈到界面。
5. 后端返回了错误响应，但前端没有展示错误状态。

## 计划
- 先梳理前后端链路与现有错误处理。
- 如需进一步运行时调试，再补充插桩。
