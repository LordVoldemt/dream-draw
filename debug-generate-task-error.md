# [OPEN] debug-generate-task-error

## 背景
- 症状：`http://127.0.0.1:5174/api/generate/tasks` 后端老是报错
- 目标：增加详细运行时日志，定位真实报错点与上下文
- 当前阶段：假设与插桩

## 复现路径
1. 启动前后端
2. 从前端触发“立即生成”
3. 观察后端接口 `/api/generate/tasks` 的报错日志

## 假设
1. 请求在进入任务处理前就因为参数校验失败或鉴权失败而返回异常
2. `process_generation_task(...)` 内部某个仓储/计费/状态更新步骤抛错
3. 图片生成或下载阶段抛出 `AppError`，但当前日志上下文不足，无法看到 provider/任务号/阶段
4. 存储作品或读取任务结果时抛出未捕获异常，导致接口表现为“后端老是报错”
5. 存在非 `AppError` 的异常，没有记录完整 traceback 和请求上下文

## 计划
- 在不改业务逻辑的前提下，先给 `/api/generate/tasks` 路由与任务执行链路增加详细日志
- 记录 task_id、user_id、payload 摘要、阶段、异常类型、异常栈
- 用户复现一次后，根据日志证据判断根因

## 证据
- 暂无，等待插桩后复现

## 下一步
- 添加 instrumentation 日志
- 请用户复现并回传日志
